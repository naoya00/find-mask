.maskform
  .maskform__en
    Add mask
  .maskform__ja
    マスク情報追加
  .maskform__box
    = form_with model:@mask, local: true do |f|
      .field
        = f.label :"製品名"
        %br/
        = f.text_field :name,class: "maskform__box--name", placeholder:"例：超快適マスク プリーツタイプ ふつう50枚"
      .field
        = f.label :image, :"外観写真" do
          = icon('fas', 'image', class: 'icon')
          = f.file_field :image
          %span.select__image--text マスクのイメージを追加できます
      .field
        = f.label :"在庫数(箱)"
        %br/
        = f.number_field  :stock,max:1000,min:1,class: "maskform__box--stock",placeholder: "在庫数を入力してください"
      = f.submit "Add mask", class: "maskform__box--submit"
      